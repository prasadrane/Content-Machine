anthropic==1.4.0
# openai pin dropped 2026-09-09: gateway has no OpenAI-protocol path (spikes/RESULTS.md);
# only consumer was spikes/spike_structured_outputs.py qwen_client() (function-local import, spike completed).
# >= pins (not ==): serverless runtimes may resolve newer pythons where
# exact pins lack prebuilt wheels (pydantic-core maturin source builds).
pydantic>=2.11.7
pydantic-settings>=2.14.1
requests>=2.32.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
numpy>=1.26
openpyxl>=3.1
# Optional, NOT installed by default (heavy; serverless never needs it):
# lazy function-level imports in connectors/linkedin_session.py + linkedin_public.py.
# For local browser session sync / zero-auth bridge:
#   pip install playwright>=1.40 && playwright install chromium
